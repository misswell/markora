<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- App Store requires sandbox -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Read/write files the user picks via NSOpenPanel/NSSavePanel -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Persist folder access across launches via security-scoped bookmarks -->
    <key>com.apple.security.files.bookmarks.app-scope</key>
    <true/>

    <!-- Google Fonts (CSP allows fonts.googleapis.com / fonts.gstatic.com) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow downloads to the user's Downloads folder -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
</dict>
</plist>
